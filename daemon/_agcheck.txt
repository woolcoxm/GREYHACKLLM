{
  "ok": true,
  "content": "// GreyLLM agent \u2014 in-game harness runtime for Grey Hack (singleplayer)\n//\n// This is the \"hands\" of the AI. The daemon (daemon/bridge.py) runs the\n// model; when the model wants to inspect or change your machine, the daemon\n// writes a tool command into /tmp/llm/command.txt and THIS program executes\n// it: listing folders, reading/writing files, running programs.\n//\n// Usage (from your in-game terminal):\n//   agent <task...>   give the AI a task; it works until done, then the\n//                         final answer is printed here\n//   agent -t          test the bridge (works even without an API key)\n//   agent -w          keep watching the current task (after a timeout)\n//   agent -r          print the last final answer\n//   agent -s <name>   save the last answer's code block as <name>.src\n//\n// Tool programs the AI writes are told to log output to the bridge's\n// out.txt file, which gets read back after each run.\n//\n// All bridge files live in /home/<you>/llm on your in-game machine.\n\nBRIDGE_DIR = home_dir + \"/.greyllm\"\nAGENT_VERSION = \"6\"\nSTALL_TIMEOUT = 900\nMARKER = \"GreyLLM-BRIDGE-READY\"\nNL = char(10)\n\nusage = function()\n\tprint(\"GreyLLM agent - in-game AI harness\")\n\tprint(\"usage: agent            interactive chat (recommended)\")\n\tprint(\"       agent <task...>  one-shot mission\")\n\tprint(\"       agent -t   test bridge\")\n\tprint(\"       agent -w   keep watching current task\")\n\tprint(\"       agent -r   show last answer\")\n\tprint(\"       agent -s <name>   save last code block\")\nend function\n\ngetComp = function()\n\treturn get_shell.host_computer\nend function\n\ngetOrCreateFolder = function(comp, path)\n\tfolder = comp.File(path)\n\tif folder != null then\n\t\treturn folder\n\tend if\n\tparts = path.split(\"/\")\n\tname = parts[parts.len - 1]\n\tresult = comp.create_folder(parent_path(path), name)\n\tfolder = comp.File(path)\n\tif folder == null then\n\t\tprint(\"ERROR: could not create \" + path + \" (\" + result + \")\")\n\tend if\n\treturn folder\nend function\n\ngetOrCreateFile = function(comp, path)\n\tf = comp.File(path)\n\tif f != null then\n\t\treturn f\n\tend if\n\tparts = path.split(\"/\")\n\tname = parts[parts.len - 1]\n\tresult = comp.touch(parent_path(path), name)\n\tf = comp.File(path)\n\tif f == null then\n\t\tprint(\"ERROR: could not create \" + path + \" (\" + result + \")\")\n\tend if\n\treturn f\nend function\n\nreply = function(comp, ok, text, tag)\n\t// the tag makes each round's completion unique \u2014 a stale \"done\" from a\n\t// previous tool round must never satisfy the daemon's next wait\n\tresultF = comp.File(BRIDGE_DIR + \"/command_result.txt\")\n\tstatusF = comp.File(BRIDGE_DIR + \"/command_status.txt\")\n\tif resultF == null then\n\t\treturn null\n\tend if\n\tif statusF == null then\n\t\treturn null\n\tend if\n\tresultF.set_content(text)\n\tif tag == null then\n\t\ttag = \"0\"\n\tend if\n\tif ok then\n\t\tstatusF.set_content(\"done \" + tag)\n\telse\n\t\tstatusF.set_content(\"error \" + tag)\n\tend if\n\treturn null\nend function\n\nexecute = function(comp, cmd)\n\t// NOTE: split() is regex-based in GreyScript \u2014 \";\" has no regex meaning.\n\t// Format: op;arg1;arg2;tag \u2014 the tag is always the last field.\n\tparts = cmd.split(\";\")\n\top = parts[0]\n\ttag = parts[parts.len - 1]\n\n\tif op == \"list\" then\n\t\tif parts.len < 2 then\n\t\t\treply(comp, false, \"list needs a path\", tag)\n\t\t\treturn null\n\t\tend if\n\t\tfolder = comp.File(parts[1])\n\t\tif folder == null then\n\t\t\treply(comp, false, \"not found: \" + parts[1], tag)\n\t\t\treturn null\n\t\tend if\n\t\trows = \"\"\n\t\tfiles = folder.get_files\n\t\tif files != null then\n\t\t\tfor f in files\n\t\t\t\trows = rows + \"FILE \" + f.path + NL\n\t\t\tend for\n\t\tend if\n\t\tfolders = folder.get_folders\n\t\tif folders != null then\n\t\t\tfor f in folders\n\t\t\t\trows = rows + \"DIR \" + f.path + NL\n\t\t\tend for\n\t\tend if
-- 0.02s
